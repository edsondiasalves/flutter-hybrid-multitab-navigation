import 'package:flutter/material.dart';

import 'red_tab_navigator.dart';

class RedTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: RedTabNavigator(),
    );
  }
}
