import 'package:flutter/material.dart';

import 'green_tab_navigator.dart';

class GreenTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: GreenTabNavigator(),
    );
  }
}
