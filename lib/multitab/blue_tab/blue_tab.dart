import 'package:flutter/material.dart';

import 'blue_tab_navigator.dart';

class BlueTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: BlueTabNavigator(),
    );
  }
}
